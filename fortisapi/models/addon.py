"""fortisapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from fortisapi.api_helper import APIHelper


class Addon(object):
    """Implementation of the 'Addon' model.

    Attributes:
        title (str): Title
        secret (str): Secret
        iframe_url (str): Iframe URL
        location_setup_url (str): Location Setup URL
        user_setup_url (str): User Setup URL
        encrypt_url_params (bool): Encrypt URL Params
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "title": "title",
        "secret": "secret",
        "iframe_url": "iframe_url",
        "location_setup_url": "location_setup_url",
        "user_setup_url": "user_setup_url",
        "encrypt_url_params": "encrypt_url_params",
    }

    _optionals = [
        "title",
        "secret",
        "iframe_url",
        "location_setup_url",
        "user_setup_url",
        "encrypt_url_params",
    ]

    _nullables = [
        "location_setup_url",
        "user_setup_url",
    ]

    def __init__(
        self,
        title=APIHelper.SKIP,
        secret=APIHelper.SKIP,
        iframe_url=APIHelper.SKIP,
        location_setup_url=APIHelper.SKIP,
        user_setup_url=APIHelper.SKIP,
        encrypt_url_params=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Addon instance."""
        # Initialize members of the class
        if title is not APIHelper.SKIP:
            self.title = title
        if secret is not APIHelper.SKIP:
            self.secret = secret
        if iframe_url is not APIHelper.SKIP:
            self.iframe_url = iframe_url
        if location_setup_url is not APIHelper.SKIP:
            self.location_setup_url = location_setup_url
        if user_setup_url is not APIHelper.SKIP:
            self.user_setup_url = user_setup_url
        if encrypt_url_params is not APIHelper.SKIP:
            self.encrypt_url_params = encrypt_url_params

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        title =\
            dictionary.get("title")\
            if dictionary.get("title")\
                else APIHelper.SKIP
        secret =\
            dictionary.get("secret")\
            if dictionary.get("secret")\
                else APIHelper.SKIP
        iframe_url =\
            dictionary.get("iframe_url")\
            if dictionary.get("iframe_url")\
                else APIHelper.SKIP
        location_setup_url =\
            dictionary.get("location_setup_url")\
            if "location_setup_url" in dictionary.keys()\
                else APIHelper.SKIP
        user_setup_url =\
            dictionary.get("user_setup_url")\
            if "user_setup_url" in dictionary.keys()\
                else APIHelper.SKIP
        encrypt_url_params =\
            dictionary.get("encrypt_url_params")\
            if "encrypt_url_params" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(title,
                   secret,
                   iframe_url,
                   location_setup_url,
                   user_setup_url,
                   encrypt_url_params,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _title=(
            self.title
            if hasattr(self, "title")
            else None
        )
        _secret=(
            self.secret
            if hasattr(self, "secret")
            else None
        )
        _iframe_url=(
            self.iframe_url
            if hasattr(self, "iframe_url")
            else None
        )
        _location_setup_url=(
            self.location_setup_url
            if hasattr(self, "location_setup_url")
            else None
        )
        _user_setup_url=(
            self.user_setup_url
            if hasattr(self, "user_setup_url")
            else None
        )
        _encrypt_url_params=(
            self.encrypt_url_params
            if hasattr(self, "encrypt_url_params")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"title={_title!r}, "
            f"secret={_secret!r}, "
            f"iframe_url={_iframe_url!r}, "
            f"location_setup_url={_location_setup_url!r}, "
            f"user_setup_url={_user_setup_url!r}, "
            f"encrypt_url_params={_encrypt_url_params!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _title=(
            self.title
            if hasattr(self, "title")
            else None
        )
        _secret=(
            self.secret
            if hasattr(self, "secret")
            else None
        )
        _iframe_url=(
            self.iframe_url
            if hasattr(self, "iframe_url")
            else None
        )
        _location_setup_url=(
            self.location_setup_url
            if hasattr(self, "location_setup_url")
            else None
        )
        _user_setup_url=(
            self.user_setup_url
            if hasattr(self, "user_setup_url")
            else None
        )
        _encrypt_url_params=(
            self.encrypt_url_params
            if hasattr(self, "encrypt_url_params")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"title={_title!s}, "
            f"secret={_secret!s}, "
            f"iframe_url={_iframe_url!s}, "
            f"location_setup_url={_location_setup_url!s}, "
            f"user_setup_url={_user_setup_url!s}, "
            f"encrypt_url_params={_encrypt_url_params!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
